Reproducing the data is automated, but formatting for
gnuplot still needs some work at the command line.

  1. Create the directory ~/tmp/data or change
     the output directory in gen_param_data.gp

  2. gp -q gen_param_data.gp

  3. Reformat the output files with one data point
     per line and a blank line between parameter
     sets with distinct n.

  * You've now reproduced
      data/enum_hrss.dat
      data/enum_hrss_prq.dat
      data/enum_pow2.dat
      data/enum_prime.dat
      data/enum_sntrup.dat
      data/sieve_hrss.dat
      data/sieve_hrss_prq.dat
      data/sieve_pow2.dat
      data/sieve_prime.dat
      data/sieve_sntrup.dat

  To get different shapes for the different weight
  parameters there's still a bit of work to do.

  4. Create the files

      enum_pow2_12.dat
      enum_pow2_23.dat
      enum_pow2_35.dat
      enum_pow2_38.dat

      enum_prime_12.dat
      enum_prime_23.dat
      enum_prime_35.dat
      enum_prime_38.dat

      sieve_pow2_12.dat
      sieve_pow2_23.dat
      sieve_pow2_35.dat
      sieve_pow2_38.dat

      sieve_prime_12.dat
      sieve_prime_23.dat
      sieve_prime_35.dat
      sieve_prime_38.dat

  5. Copy appropriate data to the files. Start with
     sort -k 3,3 enum_pow2.dat -o tmp_enum_pow2.dat
